<div class="myGallery">
<% @user.clothes.each do |specs| %>
  <%if specs.quadrant==quad%>
     <div class="item">
     <%= link_to edit_clothe_path(specs) do %>
      <%= image_tag(specs.photo, style:"border-radius:50%;") if specs.photo.attached? %>
      <% end %>
      <span class="caption"> <%= specs.quadrant %>
        <%= specs.clothing_category %>
        <%= specs.occasion %></span>
      </div>
  <% end %>  
<% end %>
</div>